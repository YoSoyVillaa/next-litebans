export const siteConfig = {
  title: "Next Litebans",
  logo: "/logo.svg",
  languages: {
    available: [
      "en",
      "es",
    ],
    default: "en",
  }
}
export type SiteConfig = typeof siteConfig;